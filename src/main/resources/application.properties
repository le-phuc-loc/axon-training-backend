quarkus.hibernate-orm.database.generation=drop-and-create
# datasource configuration
quarkus.datasource.db-kind = postgresql
quarkus.datasource.username = postgres
quarkus.datasource.password = Axon@1234#
quarkus.datasource.jdbc.url = jdbc:postgresql://localhost:5432/training

# drop and create the database at startup (use `update` to only update the schema)

quarkus.http.cors=true
quarkus.http.origins=*
quarkus.http.headers=accept, authorization, content-type, x-requested-with
quarkus.http.methods=GET, OPTIONS